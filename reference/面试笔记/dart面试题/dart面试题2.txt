好，这里我给你一份**「Dart 基础面试题大全（偏中高级，贴近 Flutter 实战）」**。
不是那种背 API 的，而是**面试官真正爱问、能拉开水平差距的**。

我会按 **重要程度 + 思维深度** 来分层，你可以直接当「复习大纲」。

---

# 一、Dart 语言基础（必问）

## 1️⃣ Dart 是强类型还是弱类型？是静态还是动态？

**答：**

* **强类型**
* **静态类型（但支持类型推断）**

```dart
var a = 1;      // 编译期推断为 int
a = 'hello';   // ❌ 编译错误
```

📌 Dart 在 **编译期就做类型检查**（AOT/JIT 都是）

---

## 2️⃣ `var`、`dynamic`、`Object` 的区别？

| 类型        | 编译期检查      | 运行期检查    |
| --------- | ---------- | -------- |
| `var`     | ✅ 有（推断后固定） | ✅        |
| `Object`  | ✅ 有        | ❌（需强转）   |
| `dynamic` | ❌ 没有       | ✅（运行时报错） |

```dart
dynamic a = 'x';
a.foo(); // 编译通过，运行崩

Object b = 'x';
b.foo(); // 编译直接报错
```

👉 **dynamic 是“放弃类型系统”**

---

## 3️⃣ Dart 中一切都是对象吗？

**是的**

```dart
int a = 1;
a.isEven;
```

* int / double / bool 都是对象
* 没有 Java 那种 primitive

---

# 二、函数 & 语法糖（高频）

## 4️⃣ Dart 有哪些函数写法？

```dart
// 普通函数
int add(int a, int b) => a + b;

// 匿名函数
var f = (int x) => x * 2;

// 可选参数
void foo(int a, [int? b]) {}
void bar({required int a, int b = 0}) {}
```

📌 **命名参数在 Flutter 里是灵魂**

---

## 5️⃣ 可选参数 `[]` 和 `{}` 的区别？

* `[]`：位置可选
* `{}`：命名可选（Flutter 几乎只用这个）

```dart
Widget({Key? key, required this.child})
```

---

## 6️⃣ 箭头函数有什么限制？

```dart
=> 只能有一行表达式
```

❌ 不能写多行逻辑
❌ 不能写 `return`

---

# 三、空安全（Null Safety，必杀题）

## 7️⃣ `String?` 和 `String` 区别？

* `String`：**永远不为 null**
* `String?`：可以为 null

```dart
String a = null;   // ❌
String? b = null;  // ✅
```

---

## 8️⃣ `!`、`?`、`??`、`??=` 区别？

```dart
a!       // 我保证不为 null（骗编译器）
a?.b     // null 传播
a ?? b   // 空合并
a ??= b  // 如果 a 为 null，赋值
```

📌 `!` 是**风险点**，面试官爱问你“什么时候用”

---

## 9️⃣ late 关键字的作用？

```dart
late String name;
```

* 延迟初始化
* 编译期保证“使用前一定会赋值”

❌ 用错会 **运行期崩溃**

---

# 四、类 & 面向对象（重点）

## 🔟 Dart 中类的构造函数有哪些？

```dart
class A {
  int x;

  A(this.x);              // 默认
  A.named(this.x);        // 命名
  A.fromJson(Map m) : x = m['x']; // 初始化列表
}
```

---

## 11️⃣ `const` 构造函数的作用？

```dart
const A(this.x);
```

* 编译期常量
* **内存复用**
* Flutter Widget 性能关键点

---

## 12️⃣ `extends` vs `implements` vs `with`

| 关键字          | 含义    |
| ------------ | ----- |
| `extends`    | 继承实现  |
| `implements` | 只实现接口 |
| `with`       | 混入代码  |

```dart
class A extends B with C implements D {}
```

📌 **implements 不继承字段，只继承行为（你刚问过）**

---

## 13️⃣ mixin 是什么？和继承的区别？

```dart
mixin Log {
  void log() {}
}
```

* 解决单继承限制
* **不持有状态（推荐）**
* 更像 trait

---

# 五、集合（必问）

## 14️⃣ List / Set / Map 的区别？

| 类型   | 特点        |
| ---- | --------- |
| List | 有序、可重复    |
| Set  | 无序、不重复    |
| Map  | key-value |

---

## 15️⃣ List 的常用高阶方法？

```dart
map
where
fold
reduce
any / every
```

📌 面试官爱让你**手写 filter / map**

---

# 六、异步（重灾区，重点）

## 16️⃣ Dart 是单线程吗？

**是（UI Isolate）**

但：

* 支持多 Isolate
* 非阻塞 IO

---

## 17️⃣ `Future` 和 `async / await` 的本质？

```dart
async / await 是 Future 的语法糖
```

```dart
Future<int> f() async {
  return 1;
}
```

等价于：

```dart
Future<int> f() {
  return Future.value(1);
}
```

---

## 18️⃣ Future.microtask 和 Future 区别？

* `microtask` 优先级更高
* 先于 event queue 执行

📌 Flutter 帧调度相关

---

## 19️⃣ Isolate 和线程的区别？

| 对比 | Isolate |
| -- | ------- |
| 内存 | 不共享     |
| 通信 | message |
| 安全 | 无锁      |

👉 Dart **用隔离换安全**

---

# 七、泛型 & 类型系统（加分项）

## 20️⃣ Dart 泛型是运行期还是编译期？

* **编译期泛型**
* 运行期有类型信息（部分擦除）

```dart
List<int> a;
```

---

## 21️⃣ `T extends Object` 和 `T extends Object?`

* 前者：T 不能为 null
* 后者：T 可以为 null

---

# 八、Dart & Flutter 关联题（面试官最爱）

## 22️⃣ Widget 为什么是不可变的？

* UI = 状态的函数
* 方便 diff
* 提升性能

---

## 23️⃣ 为什么大量 Widget 构造函数是 const？

* 减少 rebuild
* 内存复用
* 编译期优化

---

## 24️⃣ StatefulWidget 的 state 为什么单独拆？

* Widget 是配置
* State 是可变数据
* 解耦生命周期

---

# 九、常见“坑题”

## 25️⃣ `==` 和 `identical` 区别？

* `==`：可重载
* `identical`：内存地址

---

## 26️⃣ `final` vs `const`

| 关键字   | 时机  |
| ----- | --- |
| final | 运行期 |
| const | 编译期 |

---

## 27️⃣ `switch` 支持什么类型？

* int
* String
* enum

❌ 不支持范围

---

# 十、终极总结（面试一句话）

> Dart 是一门 **强类型、静态类型、单线程、基于 Isolate 的语言**，通过 **async/await、mixin、const 构造函数、空安全** 来在性能与开发效率之间取得平衡。

---

如果你愿意，下一步我可以帮你做三件事之一（**都很实战**）：

1️⃣ **Dart 面试“必背 50 问速记版”**
2️⃣ **Dart + Flutter 高频追问连环问（面试官视角）**
3️⃣ **把 Dart 和 Java / Kotlin / JS 做一一对照（你背景最合适）**

你选一个，我直接给你出“面试级答案”。
