// Top-level build file where you can add configuration options common to all sub-projects/modules.

ext {
    app_name = "==this is my custom var=="


    app_info = [
            compileSdkVersion : 28,
            minSdkVersion : 21,
            targetSdkVersion : 28,
            debug_minifyEnabled : true,
            release_minifyEnabled : true,
    ]
}