<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/image_toast_bg"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_84"
        android:scaleType="fitXY"
        android:contentDescription="@string/srl_footer_failed"
        android:src="@drawable/image_toast_bg"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="@+id/image_toast_bg"
        app:layout_constraintEnd_toEndOf="@+id/image_toast_bg"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/image_toast_bg"
        tools:ignore="UseCompoundDrawables">

        <ImageView
            android:id="@+id/image_toast_status"
            android:layout_width="@dimen/dp_35"
            android:layout_height="@dimen/dp_35"
            android:scaleType="fitXY"
            android:contentDescription="@string/srl_footer_failed"
            android:src="@drawable/icon_toast_success"
            tools:src="@drawable/icon_toast_success" />

        <TextView
            android:id="@+id/text_toast_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/dp_10"
            android:textSize="@dimen/sp_14"
            android:textColor="@android:color/white"
            tools:text="实例文字" />

    </LinearLayout>


</android.support.constraint.ConstraintLayout>